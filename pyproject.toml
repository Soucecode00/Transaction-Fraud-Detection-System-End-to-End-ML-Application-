[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fraud-detection"
version = "0.1.0"
description = "Fraud detection experimentation project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]